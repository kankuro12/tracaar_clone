<%- include('partials/head', { title: 'Invoices' }) %>
  <%- include('partials/topbar') %>
  <div class="d-flex">
    <%- include('partials/nav', { active }) %>
    <main class="admin-main flex-grow-1 p-4 bg-body-tertiary" style="min-height: calc(100vh - 56px)">
      <h1 class="h4 mb-4">Invoices</h1>
      <%- include('partials/pager', { page, pages, total, baseUrl: '/admin/invoices' }) %>
      <div class="card">
        <div class="table-responsive">
          <table class="table table-hover align-middle mb-0">
            <thead class="table-light"><tr><th>Customer</th><th>Period</th><th>Amount</th><th>Status</th><th class="text-end"></th></tr></thead>
            <tbody>
              <% invoices.forEach((i) => { %>
              <tr>
                <td class="fw-medium"><%= i.customer %></td>
                <td class="font-monospace small"><%= i.period_start %> → <%= i.period_end %></td>
                <td><%= money(i.amount) %></td>
                <td><%= i.status === 'paid' ? '<span class="badge text-bg-success">Paid</span>' : '<span class="badge text-bg-warning">Open</span>' %></td>
                <td class="text-end"><% if (i.status === 'open') { %><button class="btn btn-sm btn-outline-dark" data-pay="<%= i.id %>">Mark paid</button><% } %></td>
              </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </div>
      <div class="mt-3"><%- include('partials/pager', { page, pages, total, baseUrl: '/admin/invoices' }) %></div>
    </main>
  </div>
  <%- include('partials/scripts') %>
  <script>
    $('[data-pay]').on('click', async function () {
      try { await api(`/api/invoices/${this.dataset.pay}/pay`, { method: 'POST' }); location.reload(); }
      catch (err) { showErr('.admin-main', err.message); }
    });
  </script>
</body>
</html>
